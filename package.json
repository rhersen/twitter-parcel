{
  "name": "twitter-parcel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "browserslist": [
    "> 2%"
  ],
  "scripts": {
    "dev": "echo dev",
    "build": "rm -Rf dist && mkdir dist && cp index.html s.css index.js fetchAndShowTweets.js renderTweet.js users.js dist/",
    "postbuild": "cd functions/fauna && npm install && cd - && cd functions/twitter && npm install"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-plugin-prettier": "^3.1.4",
    "prettier": "^1.18.2"
  },
  "dependencies": {}
}
